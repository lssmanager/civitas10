const ORG=/^[A-Za-z0-9_-]{1,128}$/; const PLACEHOLDER=/:([A-Za-z][A-Za-z0-9_]*)/g;
export function buildOrganizationModuleRoute({ organizationId, template, params={}, expectedOrganizationId }:{ organizationId:string; template:string; params?:Record<string,string|number>; expectedOrganizationId?:string }){ if(expectedOrganizationId && organizationId!==expectedOrganizationId) throw new Error("cross_tenant_route_reference"); if(!ORG.test(organizationId) || organizationId.includes('/') || organizationId.includes('\\')) throw new Error("organization_id_invalid"); if(!template.startsWith('/o/:organizationId/') || template.includes('..') || /^https?:|^\/\//.test(template) || /javascript:|data:/i.test(template)) throw new Error("route_template_invalid"); const href=template.replace(':organizationId', encodeURIComponent(organizationId)).replace(PLACEHOLDER, (placeholder,name)=>{ const v=params[name]; if(v===undefined || v===null || String(v)==='' || String(v).includes('/') || String(v).includes('\\') || String(v)===placeholder) throw new Error(`route_param_invalid:${name}`); return encodeURIComponent(String(v)); }); if(PLACEHOLDER.test(href) || /\{[^}]+\}/.test(href)) throw new Error("unresolved_route_placeholder"); return href; }
