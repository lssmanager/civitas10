import { routeCatalog } from "../../../navigation/route-catalog";
import { defineScreen } from "../../../authorization/registry/define-screen";
export const ownerWorkerQueuesScreen = defineScreen({ screenId: "owner-worker-queues", capability: "owner", route: routeCatalog.ownerWorkerQueues, navigation: { menuKey: "owner.runtime.worker_queues", labelKey: "navigation.owner.runtime.worker_queues", breadcrumbKey: "breadcrumbs.owner.runtime.worker_queues", iconKey: "operations", responsiveGroup: "operations", order: 30 }, access: { requiredAllPermissions: ["owner.system.read"], requiresOrganizationContext: false }, featureFlag: "owner-runtime", organizationCustomization: { visibility: "locked", order: "locked" }, actions: ["owner.system.refresh"] });
