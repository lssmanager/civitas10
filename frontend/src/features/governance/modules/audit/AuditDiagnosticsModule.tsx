import { SectionCard } from "../../../../shared/ui";
import type { GovernanceAuditEvent } from "../../contracts";
export const AuditDiagnosticsModule = ({ events }: { events: readonly GovernanceAuditEvent[] }) => <SectionCard title="Audit and diagnostics" description="Governance writes must be audited by their owning backend contract with actor, org, target, before/after, reason and version."><div className="space-y-3">{events.map((event) => <article key={event.id} className="civitas-card civitas-pad-tight"><h3 className="font-semibold">{event.action}</h3><p className="text-sm text-slate-600">actor={event.actorId} · target={event.target} · org={event.organizationId}</p><p className="text-xs text-slate-500">{event.reason} · {event.contractVersion} · {event.createdAt}</p></article>)}</div>{events.length === 0 ? <p className="text-sm text-slate-600">No audit events were returned by the governance read model.</p> : null}</SectionCard>;
