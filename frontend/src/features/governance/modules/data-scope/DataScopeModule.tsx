import { SectionCard, StatusPill } from "../../../../shared/ui";
import type { GovernanceDataScopeAssignment } from "../../contracts";
export const DataScopeModule = ({ assignments }: { assignments: readonly GovernanceDataScopeAssignment[] }) => <SectionCard title="Data-scope assignments" description="Assignments reference taxonomy/unit IDs; resource filtering stays server-side."><div className="space-y-3">{assignments.map((assignment) => <article key={`${assignment.principalId}-${assignment.capability}`} className="civitas-card civitas-pad-tight"><div className="flex items-start justify-between gap-3"><div><h3 className="font-semibold">{assignment.principalId}</h3><p className="text-sm text-slate-600">{assignment.capability}: {assignment.resourceSummary}</p><p className="text-xs text-slate-500">taxonomy={assignment.taxonomyIds.join(",") || "-"} · units={assignment.unitIds.join(",") || "-"}</p></div><StatusPill status={assignment.effective ? "success" : "warning"}>{assignment.reason}</StatusPill></div></article>)}</div>{assignments.length === 0 ? <p className="text-sm text-slate-600">No data-scope assignments were returned.</p> : null}</SectionCard>;
