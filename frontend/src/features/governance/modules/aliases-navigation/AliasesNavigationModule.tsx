import { SectionCard, StatusPill } from "../../../../shared/ui";
import type { GovernanceAliasNavigationPolicy } from "../../contracts";
export const AliasesNavigationModule = ({ policy }: { policy: GovernanceAliasNavigationPolicy }) => <SectionCard title="Aliases and navigation" description="Aliases and menu preferences are tenant-editable only when Owner policy allows; they never authorize routes or APIs."><div className="flex flex-wrap gap-2"><StatusPill status={policy.aliasesTenantEditable ? "success" : "neutral"}>aliases tenant-editable: {String(policy.aliasesTenantEditable)}</StatusPill><StatusPill status={policy.navigationTenantEditable ? "success" : "neutral"}>navigation tenant-editable: {String(policy.navigationTenantEditable)}</StatusPill></div><div className="mt-4 space-y-2">{policy.visualPreferences.map((preference) => <div key={preference.screenId} className="flex items-center justify-between rounded-lg border border-slate-200 px-3 py-2 text-sm"><span>{preference.screenId}</span><span>{preference.locked ? "locked" : preference.hidden ? "hidden" : `order ${preference.order ?? "default"}`}</span></div>)}</div></SectionCard>;
