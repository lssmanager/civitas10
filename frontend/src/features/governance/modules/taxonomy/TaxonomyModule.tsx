import { SectionCard, StatusPill } from "../../../../shared/ui";
import type { GovernanceTaxonomyItem } from "../../contracts";
export const TaxonomyModule = ({ items }: { items: readonly GovernanceTaxonomyItem[] }) => <SectionCard title="Organization taxonomy" description="Taxonomy values are data labels/selector inputs, never roles or permission scopes."><div className="civitas-grid-3">{items.map((item) => <article key={item.id} className="civitas-card civitas-pad-tight"><h3 className="font-semibold">{item.label}</h3><p className="text-sm text-muted-strong">{item.dimension} · {item.id}</p><StatusPill status={item.status === "active" && item.assignable ? "success" : "warning"}>{item.status}{item.assignable ? "" : " / not assignable"}</StatusPill></article>)}</div>{items.length === 0 ? <p className="text-sm text-muted-strong">No taxonomy values were returned.</p> : null}</SectionCard>;
