"use strict";
const fs=require("node:fs"), path=require("node:path"); const migration=fs.readFileSync(path.join(__dirname,"../backend/db/migrations/0010_authz_data_scopes.sql"),"utf8") + "\n" + fs.readFileSync(path.join(__dirname,"../backend/db/migrations/0013_membership_role_bound_scope_subject.sql"),"utf8") + "\n" + fs.readFileSync(path.join(__dirname,"../backend/db/migrations/0014_scope_templates.sql"),"utf8");
const checks=[/scope_kind in \('dimension','unit','resource'\)/,/source_type in \('explicit','unit_membership','person_relationship','capability_group','system_migration'\)/,/status in \('scheduled','active','expired','revoked','invalidated'\)/,/valid_until is null or valid_until > valid_from/,/foreign key \(dimension_value_id, logto_organization_id\)/,/where scope_kind = 'resource' and status in \('scheduled','active'\)/,/membership_id varchar\(128\)/,/canonical_role_id varchar\(128\)/,/authorization_scope_assignments_membership_role_idx/,/owner_scope_templates/,/tenant_scope_configurations/,/authorization_scope_assignments_template_fk/];
for(const check of checks) if(!check.test(migration)){console.error(`data scope migration check failed: ${check}`); process.exit(1)}
console.log("data scope migration check passed");
