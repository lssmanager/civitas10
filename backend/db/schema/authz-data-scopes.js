"use strict";
const { sql } = require("drizzle-orm");
const { pgTable, uuid, varchar, text, timestamp, jsonb, index } = require("drizzle-orm/pg-core");
const { organizationDimensionValues } = require("./authz-taxonomy");
const { organizationUnits } = require("./authz-units");
const ts = { createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow() };
const authorizationScopeAssignments = pgTable("authorization_scope_assignments", {
  id: uuid("id").primaryKey().defaultRandom(), logtoOrganizationId: varchar("logto_organization_id", { length: 128 }).notNull(), logtoUserId: varchar("logto_user_id", { length: 128 }).notNull(), logtoRoleId: varchar("logto_role_id", { length: 128 }).notNull(), capability: varchar("capability", { length: 80 }).notNull(), scopeKind: varchar("scope_kind", { length: 40 }).notNull(), dimensionKey: varchar("dimension_key", { length: 100 }), relationshipKey: varchar("relationship_key", { length: 100 }), dimensionValueId: uuid("dimension_value_id").references(() => organizationDimensionValues.id, { onDelete: "restrict" }), unitId: uuid("unit_id").references(() => organizationUnits.id, { onDelete: "restrict" }), resourceRef: varchar("resource_ref", { length: 180 }), sourceType: varchar("source_type", { length: 60 }).notNull(), sourceRef: varchar("source_ref", { length: 180 }), sourceVersion: varchar("source_version", { length: 80 }), status: varchar("status", { length: 32 }).notNull().default("scheduled"), assignedByLogtoUserId: varchar("assigned_by_logto_user_id", { length: 128 }).notNull(), reason: text("reason"), validFrom: timestamp("valid_from", { withTimezone: true }).notNull(), validUntil: timestamp("valid_until", { withTimezone: true }), revokedAt: timestamp("revoked_at", { withTimezone: true }), revokedByLogtoUserId: varchar("revoked_by_logto_user_id", { length: 128 }), metadata: jsonb("metadata").notNull().default(sql`'{}'::jsonb`), ...ts,
}, (t) => ({ orgUserIdx: index("authorization_scope_assignments_org_user_idx").on(t.logtoOrganizationId, t.logtoUserId, t.status), orgRoleIdx: index("authorization_scope_assignments_org_role_idx").on(t.logtoOrganizationId, t.logtoRoleId, t.capability), sourceIdx: index("authorization_scope_assignments_source_idx").on(t.sourceType, t.sourceRef), dimensionIdx: index("authorization_scope_assignments_dimension_idx").on(t.logtoOrganizationId, t.dimensionKey, t.dimensionValueId), unitIdx: index("authorization_scope_assignments_unit_idx").on(t.logtoOrganizationId, t.unitId), resourceIdx: index("authorization_scope_assignments_resource_idx").on(t.logtoOrganizationId, t.resourceRef) }));
module.exports = { authorizationScopeAssignments };
