"use strict";
const {deny,organization,dimensions,relationships,self,or,assertConstraintSafe}=require("./dataScopeConstraint");
const {DATA_SCOPE_REASON_CODES}=require("./dataScopeReasonCodes");
function composeDimensionConstraint(strategy,assignments){const clauses=[]; for(const key of strategy.requiredDimensionKeys||[]){const ids=[...new Set(assignments.filter(a=>a.scopeKind==="dimension"&&a.dimensionKey===key).map(a=>a.dimensionValueId))]; if(!ids.length) return deny(DATA_SCOPE_REASON_CODES.ASSIGNMENT_MISSING); clauses.push({dimensionKey:key,operator:"in",valueIds:ids});} return dimensions(clauses);}
function composeRelationshipConstraint(strategy,candidates){const clauses=[]; for(const key of strategy.relationshipKeys||[]){const refs=[...new Set(candidates.filter(c=>key==="academic.assigned_group"||key==="academic.assigned_course"?c.resourceRef||c.unitId:key==="academic.related_student"?c.resourceRef:null).map(c=>c.resourceRef||c.unitId).filter(Boolean))]; if(refs.length) clauses.push({relationshipKey:key,targetType:key==="academic.related_student"?"person":(key.includes("group")?"unit":"resource"),targetRefs:refs});} return clauses.length?relationships(clauses):deny(DATA_SCOPE_REASON_CODES.ASSIGNMENT_MISSING);}
function composeRolePathConstraint({strategy,organizationId,subjectId,assignments=[],candidates=[]}){ let c; if(strategy.strategy==="organization") c=organization(organizationId); else if(strategy.strategy==="dimensions") c=composeDimensionConstraint(strategy,assignments); else if(strategy.strategy==="relationships") c=composeRelationshipConstraint(strategy,candidates); else if(strategy.strategy==="self") c=self(subjectId); else c=deny(DATA_SCOPE_REASON_CODES.STRATEGY_UNKNOWN); return assertConstraintSafe(c);}
function composeAcrossRolePaths(paths){const allowed=paths.filter(p=>p.allowed&&p.constraint&&p.constraint.kind!=="deny"); if(!allowed.length) return deny(DATA_SCOPE_REASON_CODES.MULTIPLE_ROLE_PATHS_INCOMPLETE); return allowed.length===1?allowed[0].constraint:or(allowed.map(p=>p.constraint));}
module.exports={composeRolePathConstraint,composeAcrossRolePaths};
